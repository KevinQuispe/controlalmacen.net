@model  almacen.Models.proveedor
@if (Model != null && Session["Username"] != null && Session["bloquear"] != null && Session["codigoalmacen"] != null)
{
    <header class="page-header">
        <center><h3>Eliminar Proveedor</h3></center>
    </header>
    <div class="container formulario">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <h3 class="bg-danger">Esta seguro de eliminar este Proveedor?</h3>
            <table class="table table-bordered table-condensed table-responsive">
                <legend>Datos de Proveedor</legend>
                <thead>
                    <tr class="btn-success">
                        <td><strong>Ruc</strong></td>
                        <td><strong>Razon Social</strong></td>
                        <td><strong>Telefono</strong></td>
                        <td><strong>Fax</strong></td>
                        <td><strong>Rep Legal</strong></td>
                        <td><strong>Correo</strong></td>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>@Html.DisplayFor(model => model.ruc)</td>@Html.HiddenFor(model => model.ruc)
                        <td>@Html.DisplayFor(model => model.razon_social)</td>
                        <td>@Html.DisplayFor(model => model.telefono)</td>
                        <td>@Html.DisplayFor(model => model.fax)</td>
                        <td>@Html.DisplayFor(model => model.rep_legal)</td>
                        <td>@Html.DisplayFor(model => model.correo)</td>
                    </tr>
                </tbody>
            </table>
            <div class="col-md-12">
                <div class="from-group"></div>
            </div>
            <p class="pull-right">
                <input type="submit" value="Delete" class="btn btn-danger" />
                <a href="~/Proveedores/listaProveedores" class="btn btn-success"><span class="fa fa-arrow-left"></span>Volver</a>
            </p>
        }
    </div>
}
else
{
    <script type="text/javascript">
        window.open("../Home/Login", "_self");
    </script>
}

